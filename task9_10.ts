//I have stored my favourite num in the variable named "f_num";
let f_num: number = 9;
//then wrote the msg by using value in "f_num" in a variable named msg;
const msg = `My favorite number is ${f_num}.`;
//printed the msg;
console.log(msg);